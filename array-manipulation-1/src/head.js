/* exported head */
/* Psuedocode:
Want a funtion that takes an array as an argument
and returns the first element of the array.
  Define the function and pass 'array as parameter'.
  Assign a variable to the value of the first array element.
  return the variable. */

function head(array) {
  const head1 = array[0];
  return head1;
}

/* exported getFirstChar */
/* Psuedocode:
Want a function that takes a parameter, string. The return
value will be the first character of the string.
You will want to find the first character of string.
Store it
Return it */

function getFirstChar(string) {
  const firstLetter = string[0];
  return firstLetter;
}

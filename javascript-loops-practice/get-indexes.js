/* exported getIndexes */
function getIndexes(array) {
  const emptyArray = [];
  for (let i = 0; i < array.length; i++) {
    const forResult = i;
    emptyArray.push(forResult);
  }
  return emptyArray;
}

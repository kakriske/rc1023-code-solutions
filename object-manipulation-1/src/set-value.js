/* exported setValue */

/* Psuedocode:
Write a function that takes three parameters. The parameters
are object, key, and value. The return value will not return anything.
The function will add the key and value to the object parameter.
  Define the function and pass the three parameters.
  Set the key of the object to the value. */

function setValue(object, key, value) {
  object[key] = value;
}
